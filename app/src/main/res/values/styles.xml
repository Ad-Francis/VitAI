<resources>
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="MyCustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">44sp</item> <!-- Adjust text size as needed -->
        <item name="android:textColor">@color/white</item> <!-- Optional: adjust text color -->
    </style>

    <style name="CustomTabLayout" parent="Widget.MaterialComponents.TabLayout">
        <item name="tabTextColor">@color/white</item> <!-- Unselected text color -->
        <item name="tabSelectedTextColor">@color/light_gray</item> <!-- Selected text color -->
        <item name="tabIndicatorColor">@color/teal_200</item> <!-- Indicator color if needed -->
    </style>

</resources>